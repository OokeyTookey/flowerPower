using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraChangeShiftTest : MonoBehaviour
{
    /*private Vector3 currentCameraPosition;
    public GameObject mainCamera;
    public GameObject playerReference;
    public GameObject desiredLocation;

    private void Start()
    {
        currentCameraPosition = mainCamera.transform.position;
    }

    private void OnTriggerStay(Collider other)
    {
        Vector3 cameraOffset = playerReference.position + currentCameraPosition;

        if (other.CompareTag("Player"))
        {
            mainCamera.transform.position = Vector3.Lerp(mainCamera.transform.position, desiredLocation.transform.position, 0.1f);
            mainCamera.transform.rotation = Quaternion.Slerp(mainCamera.transform.rotation, desiredLocation.transform.rotation, 0.1f);
            transform.position = Vector3.Lerp(transform.position, cameraOffset, 0.1f);
        }
    }

    private void OnTriggerExit(Collider other)
    {
        //transform.position
    }*/
}
